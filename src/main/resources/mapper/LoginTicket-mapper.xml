<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--指定作用域具体接口的位置-->
<mapper namespace="com.lys.community.dao.LoginTicketDao">
    <!--<insert id="insertLoginTicket" parameterType="LoginTicket">
        insert into login_ticket (id, user_id, ticket, status, expired)
        values (0,#{user_id},#{ticket},#{status},curdate());
    </insert>

    <select id="selectLoginTicket" resultType="LoginTicket" parameterType="String">
        select id,user_id,ticket,status,expired
        from login_ticket
        where ticket = #{ticket};
    </select>

    <update id="updateStatus">
        update login_ticket
        set status=#{status}
        where ticket=#{ticket}
        <if test="ticket!=null">
            and 1=1
        </if>
    </update>
-->

</mapper>